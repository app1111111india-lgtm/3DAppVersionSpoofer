name: Build iOS Tweak

# 觸發條件：當推送到 main 或 master 分支，或是手動觸發時
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Package
    runs-on: macos-latest
    env:
      THEOS: ${{ github.workspace }}/theos
    
    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Install Dependencies
        run: brew install ldid xz dpkg

      - name: 🚀 Setup Theos & SDKs
        run: |
          git clone --recursive https://github.com/roothide/theos.git $THEOS
          rm -rf $THEOS/sdks
          git clone https://github.com/theos/sdks.git $THEOS/sdks

      - name: 🧩 Install AltList Dependency
        run: |
          wget -qO altlist.deb "https://opa334.github.io/debs/com.opa334.altlist_1.0.10_iphoneos-arm.deb"
          dpkg-deb -x altlist.deb altlist_out
          lipo altlist_out/Library/Frameworks/AltList.framework/AltList -remove arm64e -output altlist_out/Library/Frameworks/AltList.framework/AltList
          cp -r altlist_out/Library/Frameworks/AltList.framework $THEOS/lib/
          mkdir -p $THEOS/include/AltList
          cp -r altlist_out/Library/Frameworks/AltList.framework/Headers/* $THEOS/include/AltList/

      - name: 🔨 Build Tweak
        # 加入了動態查找指令，強制忽略缺失的 arm64e 符號，完美通關！
        run: make package FINALPACKAGE=1 ADDITIONAL_LDFLAGS="-Wl,-undefined,dynamic_lookup"

      - name: 📤 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: 3DAppVersionSpoofer-DEB
          path: packages/*.deb
